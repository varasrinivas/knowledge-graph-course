---
# TODO 1: complete the frontmatter. Only `type` is REQUIRED by the spec,
# but fill the recommended fields too — your lint (Step 3) warns on them.
type:
---

# Weekly Active Users (WAU)

TODO: define the metric precisely (window, what counts as active, exclusions)
and link the related table concept.
